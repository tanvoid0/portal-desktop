// Kubernetes service layer for additional business logic
// Currently empty, can be extended as needed

pub struct KubernetesService;

impl KubernetesService {
    pub fn new() -> Self {
        Self
    }
}
